# ClusterCA 长线开发计划

> 版本基线：v0.2.0 · 作者：Anshonesoo  
> 更新日期：2026-08-10  
> 说明：本文件是后续长线执行的主计划，按阶段推进；每完成一个可独立审查的板块统一更新总体方案文档。

## 1. 当前状态盘点（已完成）

- 世界 2048×2048 环面；每区块 64×64 格，共 32×32 个区块。
- 三层世界（团簇/物质/光），确定性 Tick 与异常回滚。
- 防重叠碰撞：扫掠候选、接触图、链式推动、事务提交、最终断言、边/角装甲。
- 有符号运动计数器、维护、过载损伤、成功轴扣减。
- 普通群组、家庭刚体、种子、发育、基因 VM。
- 藻类 3×N 定向生长、趋光、3×6 强制分裂。
- 控制器（常量/加减/比较/逻辑/延迟/脉冲/锁存）、传感器（光/资源/邻近）。
- 编译系统：片段拼接、`!CLEAR`/`!RESET` 覆盖清空、END 检测、模板存储、生殖端口产种。
- 编辑器：工具、图层、折叠、网格、设置、新手引导、遗传模板导入/导出、开发者模板、模板 2×2（定制锁定）。
- **教学模式**：独立展示小地图布置 16 个器官演示点，教学面板（图标卡 + 说明）点击聚焦；教学内容由 `teachingMap.ts` 单一数据源驱动，可追加/修改实时生效。
- **弹性动画**：相机聚焦/回原点采用 easeOutBack 回弹；按钮、卡片、弹窗加弹性过渡与 popIn 弹出。
- 测试：12 个测试文件、82 项通过；tsc 与构建通过。

## 2. 已确认方案（已完成首轮代码对齐）

依据《元胞种类说明.md》逐类审定：

| 器官 | 方案 | 落地动作 |
|---|---|---|
| 储存器 0004 | 取消 | 移除注册表/UI/测试引用，容量与面积绑定 |
| 储存访问端口 0005 | 可配置通道 | 增加 `mode`（amount/energy/both），按通道限制开放 |
| 物质交换器 0006 | 双向分离 | `mode`（absorb/eject/both）+ `range` 预设范围吸收 |
| 防御结构 0010 | 暂不设置 | 从注册表/UI 移除或标注未开放 |
| 外部收发器 000d | 可配置距离 | 增加 `range`，群组内通信限距 |

其余器官保持现状。

## 3. 开发原则（长线不变量）

- 防重叠算法优先级最高；任意 Tick 提交后不同刚体逻辑矩形不得重叠。
- 模拟逻辑整数化、确定性：运行期不引入随机数、浮点物理差异、插入顺序依赖。
- 碰撞结果、并列裁决不依赖对象遍历顺序。
- 被阻挡轴不扣阈值；成功轴只扣一次。
- 四角始终为壳；发育只写自身合法区域；跨越环面边界不改变面积/布局/资源。
- 保存再加载后状态与继续运行结果一致。
- 自定义规则不能绕过意图验证制造重叠或负资源。
- 每完成板块补测试并同步总体方案文档。

## 4. 阶段路线

### 阶段 A：元胞方案落地（近期，v0.3）

目标：把已确认方案对齐到代码，全部带测试。

| # | 任务 | 说明 | 验收 |
|---|---|---|---|
| A1 | 取消储存器 | 移除 0004 定义、UI 选项与测试；容量公式改为纯面积绑定 | 编辑器无储存器；容量=4×area/8×area |
| A2 | 访问端口可配置通道 | 0005 支持 `mode`；按通道限制消费者读取 | 仅开放物质时能量读取为 0 |
| A3 | 交换器双向分离 | 0006 支持 `mode` 与 `range`；吸收可覆盖预设范围 | 吸收范围超出覆盖格仍生效 |
| A4 | 防御暂缓 | 移除/标注 0010 | 编辑器无防御结构，相关测试调整 |
| A5 | 收发器可配置距离 | 000d 增加 `range`，超距不通信 | 超距同组不收到信号 |
| A6 | 全量验证 | 新增测试 + 回归 | tsc / vitest / build 全绿 |

### 阶段 B：编辑器与交互完善（中期，v0.4）

| # | 任务 | 说明 | 验收 |
|---|---|---|---|
| B1 | 器官参数配置面板 | 属性栏可编辑通道/方向/force/operation/value/range 等 | 配置即时生效并计入撤销 |
| B2 | 基因/编译编辑器 | 编辑与可视化基因模板、编译片段 | 可粘贴/保存/载入完整基因 |
| B3 | 开发者模板补全 | 展示控制器/传感器/编译/交换器新能力示例 | 各功能链可单步演示 |
| B4 | 网格与画布细节 | 区域锁定、框选、批量编辑完善 | 交互符合预期 |

### 阶段 C：生态与规则扩展（中期，v0.5）

| # | 任务 | 说明 | 验收 |
|---|---|---|---|
| C1 | 控制器/传感器组合应用 | 趋光、避障、资源检测自动胞团示例 | 组合行为可运行 |
| C2 | 繁殖链完整演示 | 编译→生殖→发育→激活闭环 | 闭环可复现 |
| C3 | 初始生态细化 | 初始分布按确认方案；藻类与物质环境调优 | 生态可运行且确定 |
| C4 | 自定义规则回归 | 声明式/可信脚本规则与器官对齐 | 规则意图合法 |

### 阶段 D：验收与发布（远期，v0.6）

| # | 任务 | 说明 | 验收 |
|---|---|---|---|
| D1 | 完整验收 | 保存续跑、防重叠组合、确定性回归全覆盖 | 验收清单全过 |
| D2 | 部署更新 | 相对路径构建、本地生产服务器和静态托管说明；公共发布按用户指定目标执行 | `dist` 与 Worker 可从静态 HTTP 正常运行 |
| D3 | 文档同步 | 总体方案、元胞说明、开发计划统一 | 文档与代码一致 |

## 5. 下一步执行顺序

1. 阶段 A–D 的初版范围均已完成并通过要求级验收。
2. 后续开发以用户审查反馈为入口，不再默认扩大首版器官或性能功能。
3. 若需要公开上线，下一步仅需用户指定静态托管目标，再执行对应账号或仓库发布流程。
4. 新增需求继续遵循：规则确认 → 实现 → 单测 → 构建 → 浏览器验收 → 汇总文档。

## 6. 变更记录

- 2026-08-10：建立长线计划，明确阶段 A 为下一执行单元。
- 2026-08-10：**阶段 A 完成**（A1 取消储存器、A2 端口通道、A3 交换器双向+范围、A4 防御暂缓、A5 收发器距离、A6 全量验证；68 项测试通过）。
- 2026-08-10：**阶段 B 部分完成**（B1 器官参数面板：channel/inputChannel/value/force/operation/mode/range 可配并随放置生效；B3 开发者模板补交换器示例；B2 由遗传模板+属性栏基因编辑覆盖；B4 网格与画布细节已充分调优）。
- 2026-08-20：恢复已确认的 2048×2048 世界；每区块 64×64 格，共 32×32 个区块。
- 2026-08-20：完成真实编译处理器职责分离。处理器拼接、清空、校验并一次性输出完整 END 基因；生殖端口拒绝未经处理器输出的原始消息。
- 2026-08-20：四种新建模板全部开放；标准生态固定为 8 个确定性初始藻类；开发者模板可在两次单步内复现“编译→存储→生殖”闭环。69 项测试及浏览器验收通过。
- 2026-08-20：**阶段 C1 部分完成**。推进器、喷射器支持可选数字频道门控；开发者模板加入“邻近传感器→NOT 控制器→推进器”避障链。累计 71 项测试通过。
- 2026-08-20：**阶段 C1 完成**。传感器增加方向光差模式，交换器复用可选数字门控；开发者模板加入单轴趋光和资源限量吸收。参数面板按器官过滤有效选项，数字门控默认关闭。累计 75 项测试及浏览器验收通过。
- 2026-08-20：**阶段 C3 完成**。标准生态抽为独立确定性模板模块，固定生成 8 个藻类、256 个物质格、4 个资源斑块和中心样本；同种子摘要一致、异种子不同。累计 78 项测试及浏览器验收通过。
- 2026-08-20：**阶段 D1 完成**。复杂状态保存后连续 12 Tick 一致；新增链推、不可推动体及 200 组接触组合验收。累计 82 项测试通过。
- 2026-08-20：**阶段 D2 完成可部署产物**。Vite 使用相对资源路径；新增无依赖本地生产服务器、`serve.cmd` 和 `DEPLOY.md`，实际验证 HTML、资源、Worker 与繁殖单步运行。公共站点上传等待用户指定托管目标。
- 2026-08-20：补齐声明式自定义器官界面，定制模板可验证 JSON、注册器官并加入调色板；修复语言切换重建 Worker 导致世界重置的问题。
- 2026-08-20：**阶段 B、D3 收口**。自定义器官进入调色板并随项目保存；切换语言不再重置权威世界；新增性能空探针契约和逐项《验收清单》。初版 A–D 范围完成。
- 2026-08-20：**新增教学模式**。`src/templates/teachingMap.ts` 定义 16 个器官演示点（独立展示小地图 + 功能说明），`TeachingPanel` 提供教学面板；新增 `teach` 模板，顶部 🎓 入口。内容数据驱动，可追加/修改实时生效。
- 2026-08-20：**界面弹性动画**。相机缓动改 easeOutBack 回弹；按钮/卡片/分段/弹窗加弹性 cubic-bezier 过渡与 popIn 弹出。tsc / 82 项测试 / 构建通过。
- 2026-08-20：**繁殖规则强化**。生殖端口支持 `reproduceCostAmount`/`reproduceCostEnergy`（产种从母体扣资源，不足不产并保留重试）与 `cooldown`（产后冷却）；注册预留器官 `0013 变异器`（暂不实现行为，定位为将来在产卵前干涉基因序列）。新增 1 项测试，累计 83 项通过。
