# ClusterCA 元胞种类说明

> 版本：v0.2.0 · 作者：Anshonesoo  
> 世界：2048×2048 环面；每区块 64×64 格，共 32×32 个区块  
> 文档状态：**方案已确认并完成首轮代码对齐**

---

## 1. 总览表

| 代码 | 名称 | 分组 | 方案状态 | 一句话功能 |
|---|---|---|---|---|
| `0001` | 壳 | 结构 | 保持现状 | 四角必要结构，自动生成 |
| `0002` | 基因核心 | 结构 | 保持现状 | 保存并执行基因，发育起点 |
| `0003` | 能量转换器 | 能量 | 保持现状 | 按光强产能量 |
| `0004` | 储存器 | 能量 | 已取消 | 由胞团自带仓库替代 |
| `0005` | 储存访问端口 | 能量 | 可配置通道 | 为相邻消费者开放资源池 |
| `0006` | 物质交换器 | 物质 | 双向分离 | 吸收/输出环境资源 |
| `0007` | 数字信号端口 | 信号 | 保持现状 | 接入数字总线 |
| `0008` | 编译信号端口 | 编译 | 保持现状 | 接入编译总线 |
| `0009` | 控制器 | 信号 | 保持现状 | 整数运算输出 |
| `000a` | 传感器 | 信号 | 保持现状 | 探测本地/前方状态 |
| `000b` | 编译存储器 | 编译 | 保持现状 | 保存完整基因模板 |
| `000c` | 编译处理器 | 编译 | 保持现状 | 拼接/清空/检测基因 |
| `000d` | 外部收发器 | 通信 | 可配置距离 | 群组内广播信号 |
| `000e` | 编组器 | 群组 | 保持现状 | 自动握手成刚体群组 |
| `000f` | 推进器 | 运动 | 保持现状 | 消耗能量加运动计数 |
| `0010` | 防御结构 | 防御 | 暂不设置 | （本版本不提供） |
| `0011` | 喷射器 | 运动 | 保持现状 | 排出物质产生反冲 |
| `0012` | 生殖端口 | 编译 | 成本+冷却 | 生成休眠种子 |
| `0013` | 变异器 | 繁殖 | 预留 | 干涉卵的基因序列（未实现） |
| — | 藻类 | 特殊 | 保持现状 | 结构识别型光合生物 |

---

## 2. 结构基础

### 壳 `0001` — 保持现状

| 项目 | 说明 |
|---|---|
| 功能 | 四角必要结构，保证胞团合法与碰撞强度 |
| 放置要求 | 只允许出现在四个角；创建时自动生成，编辑器不可擦除 |
| 使用方式 | 无需手动管理；四角缺失时胞团不合法 |
| 参数 | 无 |
| 示例 | 创建任意胞团自动带四角壳 |

### 基因核心 `0002` — 保持现状

| 项目 | 说明 |
|---|---|
| 功能 | 保存当前基因；发育期遗传蚂蚁的起点 |
| 放置要求 | 发育期内部格，种子展开 3×3 时自动放置中心 |
| 使用方式 | 基因执行结束后胞团转正式激活态 |
| 参数 | 无 |
| 示例 | 种子展开后自动获得基因核心并开始执行基因 |

---

## 3. 能量系统

### 能量转换器 `0003` — 保持现状

| 项目 | 说明 |
|---|---|
| 功能 | 按所在格光强生产能量：`min(16, floor(light / 16))` |
| 放置要求 | 内部格，处于有光照位置 |
| 使用方式 | 放置即持续产能；藻类内部全由它构成 |
| 参数 | 无 |
| 示例 | 光合胞团：内部放若干转换器即可供能 |

### 储存器 `0004` — 已取消

| 项目 | 说明 |
|---|---|
| 功能 | 不再作为独立器官存在 |
| 替代 | 胞团自带仓库：物质容量 `4×area`，能量容量 `8×area`，按面积自动计算 |
| 参数 | 无 |
| 备注 | 原"加容量"能力移除，容量与面积绑定 |

### 储存访问端口 `0005` — 可配置通道

| 项目 | 说明 |
|---|---|
| 功能 | 为四邻接的资源消费者开放团簇仓库 |
| 参数 | `mode`：`amount`（只物质）/ `energy`（只能量）/ `both`（默认） |
| 放置要求 | 内部格 |
| 使用方式 | 消费者必须四邻接它才能读取仓库；按需隔离物质/能量 |
| 示例 | 推进器旁放 `mode=energy` 的端口，只放行能量消耗 |

---

## 4. 物质系统

### 物质交换器 `0006` — 双向分离

| 项目 | 说明 |
|---|---|
| 功能 | 吸收口与输出口分开配置：吸收口从指定方向/预设范围吸收物质；输出口从边界向界外排出溢出 |
| 参数 | `direction`、`mode`（`absorb`/`eject`/`both`）、`range`（吸收预设范围，可超出团簇覆盖格）、可选 `inputChannel` |
| 放置要求 | 内部格，需紧邻边界（`x=1` 等）或位于范围可达位置；需相邻储存访问端口 |
| 使用方式 | 吸收范围不再限于团簇自身覆盖格，可按 `range` 吸收预设邻域；设置 `inputChannel` 后仅在对应数字频道非零时工作 |
| 示例 | `mode=absorb, range=3` 吸收 3 格范围内环境物质 |

---

## 5. 运动系统

### 推进器 `000f` — 保持现状

| 项目 | 说明 |
|---|---|
| 功能 | 消耗 1 能量，按方向给胞团加有符号运动计数 |
| 参数 | `direction`（上下左右）、`force`（1–1024）、可选 `inputChannel`（非零时启用） |
| 放置要求 | 内部格，四邻接储存访问端口 |
| 使用方式 | 多台叠加运动计数；计数达移动阈值才移动；被阻挡不扣阈值。设置 `inputChannel` 后还需四邻接数字端口，频道值非零才工作 |
| 示例 | 能量充足时持续推进驱动胞团移动 |

### 喷射器 `0011` — 保持现状

| 项目 | 说明 |
|---|---|
| 功能 | 消耗 1 物质，向指定方向界外排出 1 物质并产生反冲动量 |
| 参数 | `direction`、可选 `inputChannel`（非零时启用） |
| 放置要求 | 内部格且紧邻边界（`x=1` 等），四邻接储存访问端口 |
| 使用方式 | 资源消耗型移动；反冲方向与喷射方向相反。设置 `inputChannel` 后使用与推进器相同的数字门控 |
| 示例 | 向 `left` 喷射 → 胞团获得向右反冲 |

---

## 6. 防御系统（暂不设置）

| 项目 | 说明 |
|---|---|
| 状态 | 防御结构 `0010` 本版本**暂不设置** |
| 保留 | 引擎的边甲/减伤能力保留，后续按需开放 |
| 参数 | 无 |

---

## 7. 信号系统

### 数字信号端口 `0007` — 保持现状

| 项目 | 说明 |
|---|---|
| 功能 | 接入胞团内部数字总线（int16，256 频道），信号下一 Tick 送达 |
| 参数 | `channel`（0–255） |
| 放置要求 | 内部格 |
| 使用方式 | 收发方都需四邻接对应端口；同频道多输入先求和再饱和到 int16 |
| 示例 | 两个常量控制器同频道 → 总线值求和 |

### 控制器 `0009` — 保持现状

| 项目 | 说明 |
|---|---|
| 功能 | 对输入做确定性整数运算后输出到总线 |
| 参数 | `operation`、`inputChannel`、`value`、`channel` |
| 运算 | `constant` / `add` / `sub` / `compare` / `and` / `or` / `not` / `delay` / `pulse` / `latch` |
| 放置要求 | 内部格，四邻接数字信号端口 |
| 使用方式 | 读 `inputChannel` 总线，结果写 `channel`；可级联 |
| 示例 | `add`：输入 1000 + value 500 → 输出 1500 |

### 传感器 `000a` — 保持现状

| 项目 | 说明 |
|---|---|
| 功能 | 探测本地状态或前方环境并输出到数字总线 |
| 参数 | `operation`、`direction` |
| 探测 | `light`（所在格光强）/ `light-gradient`（指定方向前后边界的有符号光强差）/ `energy`（本地能量）/ `amount`（本地物质）/ `proximity`（前方是否有物质或胞团） |
| 放置要求 | 内部格，四邻接数字信号端口 |
| 使用方式 | 输出接到控制器做趋光/避障/资源检测 |
| 示例 | `proximity` 前方有物质 → 输出 1 |

---

## 8. 通信系统

### 外部收发器 `000d` — 可配置距离

| 项目 | 说明 |
|---|---|
| 功能 | 在有效普通群组内向匹配频道成员广播数字信号 |
| 参数 | `channel`、`range`（通信距离，群组内仍限距可配） |
| 放置要求 | 内部格 |
| 使用方式 | 收发双方都需启用且同频道的收发器，且属同一群组；距离按 `range` 限制 |
| 示例 | `range=8` 仅 8 格内同组收发器可通信 |

---

## 9. 群组系统

### 编组器 `000e` — 保持现状

| 项目 | 说明 |
|---|---|
| 功能 | 同码相邻握手组成普通刚体群组 |
| 参数 | `value`（群组码 0–65535） |
| 放置要求 | 内部格 |
| 使用方式 | 两胞团各放同码编组器并相邻，Tick 末自动成组；整体单一刚体 |
| 示例 | 双胞团同码相邻 → 自动编组协同移动 |

---

## 10. 编译与繁殖

### 编译信号端口 `0008` — 保持现状

| 项目 | 说明 |
|---|---|
| 功能 | 接入编译信号总线（hex 基因片段与 `!` 命令），下一 Tick 送达 |
| 参数 | `channel` |
| 放置要求 | 内部格 |
| 使用方式 | 处理器/生殖端口四邻接它收发基因片段 |

### 编译处理器 `000c` — 保持现状

| 项目 | 说明 |
|---|---|
| 功能 | 片段存储、确定性拼接、`!CLEAR`/`!RESET` 覆盖清空、完整 END 基因检测 |
| 参数 | 无（读相邻编译端口频道） |
| 放置要求 | 内部格，四邻接编译信号端口 |
| 使用方式 | 片段按送达顺序拼接；完整合法 END 基因产出 |
| 示例 | 发片段 → 发完整基因 → 产出合法 END 基因 |

### 编译存储器 `000b` — 保持现状

| 项目 | 说明 |
|---|---|
| 功能 | 保存合法完整基因作为模板 |
| 参数 | 无 |
| 放置要求 | 内部格 |
| 使用方式 | 产出完整基因时自动写入最近一份模板 |
| 示例 | 繁殖后存储器保存一份可复用基因模板 |

### 生殖端口 `0012` — 产种成本与频率

| 项目 | 说明 |
|---|---|
| 功能 | 接收完整合法 END 基因，在边界外相邻格生成单格休眠种子 |
| 参数 | `direction`、`reproduceCostAmount`（每次产种扣除母体物质，默认 0）、`reproduceCostEnergy`（扣除能量，默认 0）、`cooldown`（产种后冷却 Tick，默认 0） |
| 放置要求 | 内部格且紧邻边界（如 `x=width-2`），四邻接编译信号端口 |
| 使用方式 | 与编译端口/处理器构成繁殖链；产种前校验母体资源足够，不足则不产并保留缓冲重试；成功产种后扣成本并进入冷却 |
| 示例 | `reproduceCostAmount=5` 时母体需 5 物质才能产一次 |

### 变异器 `0013` — 预留

| 项目 | 说明 |
|---|---|
| 状态 | **预留**，尚未实现行为 |
| 定位 | 用于在产卵前确定性干涉/改写卵的基因序列（变异相关暂不制作） |
| 放置要求 | 内部格（注册已生效，可被脚本/自定义规则识别；编辑器暂不直接提供，避免误用） |
| 备注 | 引擎已在繁殖产种路径预留了基因拦截点，后续按确认方案接入 |

---

## 11. 特殊生命

### 藻类 — 保持现状

| 项目 | 说明 |
|---|---|
| 功能 | 结构识别型光合生物：外围全壳、内部全光合转换器 |
| 规则 | 尺寸固定 `3×N`（N=3..6）；沿唯一最亮方向生长；`3×6` 强制分裂为两个 `3×3` |
| 参数 | 无（结构自动识别，无基因） |
| 使用方式 | 用于生态模拟、光照与分裂测试 |
| 示例 | 标准生态模板自动生成大量藻类 |

---

## 12. 常用组合示例

| 组合 | 器官链 | 效果 |
|---|---|---|
| 能量链 | 能量转换器 → 储存访问端口 → 推进器 | 产能驱动运动 |
| 信号链 | 常量控制器 → 数字端口 → 传感器/控制器 | 总线运算 |
| 避障链 | 邻近传感器 → 数字端口 → NOT 控制器 → 门控推进器 | 探测方向被占用时停止推进 |
| 单轴趋光 | 方向光差传感器 → 比较控制器 → 门控推进器 | 目标方向更亮时推进 |
| 限量吸收 | 物质量传感器 → 比较 → NOT → 门控交换器 | 超过阈值后停止继续吸收 |

---

## 13. 自定义器官

定制模板提供声明式 JSON 编辑器。每份文档同时包含一个 `definition` 和一个同 ID 的 `rule`：器官代码必须位于 `0100–FFEF`，颜色使用 `#RRGGBB`，成本和回收率为整数字符串；规则只使用已有整数状态字段、比较符，以及改变资源或增加动量动作。

提交由 Worker 事务式验证。定义或规则任一非法时恢复提交前状态；成功后器官立即加入调色板，并随 `.clusterca` 保存。复杂 JavaScript 模块不通过此编辑器运行，仍走独立的可信脚本授权流程。
| 繁殖链 | 编译信号端口 → 编译处理器 → 生殖端口 | 基因 → 休眠种子 |
| 群组型 | 两个编组器同码相邻 | 刚性联合体 |
