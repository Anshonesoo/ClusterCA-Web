# ClusterCA Web V2 初版验收清单

> 验收日期：2026-08-20  
> 世界规格：2048×2048 环面；每区块 64×64 格，共 32×32 个区块  
> 自动验证：12 个测试文件、82 项测试通过；TypeScript 类型检查和 Vite 生产构建通过

## 1. 产品目标

| 要求 | 状态 | 直接证据 |
|---|---|---|
| 轻量化 | 通过 | 纯 TypeScript/Preact 前端；生产产物由静态 HTML、CSS、主线程模块和两个 Worker 模块组成，无数据库或常驻后端 |
| 可部署至网站 | 通过 | `vite build` 生成 `dist`；资源路径为相对路径；独立静态服务器验证入口、资源、Worker 和单步运行 |
| 可下载后本地部署 | 通过 | `serve.cmd` 与 `scripts/serve-dist.mjs` 可直接提供 `dist`；`DEPLOY.md` 记录使用方式 |
| 可定制器官种类 | 通过 | 定制模板打开“器官定义 + 声明式规则”JSON 编辑器；Worker 验证后加入调色板，并随项目保存 |
| 可视化、可编辑 | 通过 | WebGL2 三层渲染；胞团、器官、物质、基因工具；选择、平移、撤销/重做、图层和运行控制 |
| 可保存和导出 | 通过 | `.clusterca` ZIP、IndexedDB 自动存档、指标 CSV、选区三层 JSON、当前视图 PNG |

## 2. 模型不变量

| 不变量 | 状态 | 直接证据 |
|---|---|---|
| 三层世界与2048×2048环面 | 通过 | 常量、界面世界信息、环面几何/光层/物质层测试 |
| 完整逻辑矩形不重叠 | 通过 | 编辑拒绝、Tick最终断言、事务式求解器、200组确定性接触组合测试 |
| 碰撞和并列裁决不依赖输入顺序 | 通过 | 求解器输入反序结果一致测试 |
| 链式推动科学约束 | 通过 | 联合动量达到联合阈值时整链推动；含不可推动体时整链停止 |
| 被阻挡轴不扣阈值、成功轴只扣一次 | 通过 | World运动计数器回归测试 |
| 四角始终为壳，普通边界为空 | 通过 | 创建、编辑与发育均通过统一边界校验 |
| 保存后精确续跑 | 通过 | 普通存档测试及包含编译队列、群组、藻类、自定义规则的复杂状态连续12 Tick一致测试 |
| 运行期确定性 | 通过 | 同状态摘要一致、模板同种子一致、自定义规则排序、信号排序及可信脚本隔离 |

## 3. 生命与器官系统

- 当前启用16类内置器官；储存器 `0004` 已取消，防御结构 `0010` 暂不开放。
- 数字/编译双总线、普通群组、家庭刚体、种子、基因发育和藻类强制分裂均已实现并测试。
- 编译处理器负责片段拼接、清空、END校验、模板保存和一次性输出；生殖端口不能读取未经处理的原始基因。
- 开发者模板包含繁殖、避障、单轴趋光、资源限量吸收、能量推进、编组、交换、喷射和藻类示例。
- 标准生态固定生成8个藻类、256个物质格、4个资源斑块；固定种子完全复现。

## 4. 扩展安全

- 声明式规则只允许整数条件与“改变资源/增加动量”意图，所有结果在权威世界中验证。
- 自定义器官注册是Worker事务：定义或规则任一失败会恢复注册前世界。
- 可信脚本随项目载入后默认禁用，必须由用户明确授权；运行在独立Worker中并受超时和意图验证约束。
- 切换语言或界面设置不会重建权威模拟Worker，当前Tick、胞团和种子保持不变。

## 5. 性能功能边界

初版不实现性能测试或性能面板，也不采集性能样本。仅保留 `PerformanceProbe` 空接口；未来若启用，单个样本必须同时包含活跃物质格数、胞团数量和平均胞团面积，不能只记录地图尺寸。

## 6. 部署边界

已经证明当前 `dist` 可在本地静态HTTP服务器完整运行，也可上传至普通静态托管。尚未替用户发布到具体GitHub Pages、Cloudflare Pages、Netlify或Vercel账号，因为当前没有指定目标站点和发布凭据；这不影响“产物可部署”的验收结论。

## 7. 标准验证命令

```powershell
pnpm test
pnpm build
pnpm serve:dist
```

Windows下载项目也可直接双击 `serve.cmd` 启动已构建版本。
