# ClusterCA Web V2 — UI 换新开发计划

> 文档状态：实施中（已加入设置系统、版本号与作者署名）  
> 更新日期：2026-08-05  
> 范围：界面视觉与交互全面换新，编辑器底层能力逐步接入
> 版本：v0.2.0  
> 作者：Anshonesoo

## 1. 背景与目标

当前界面已经具备可操作的三栏框架（工具、画布、检查器）和完整 Worker 模拟接线，但视觉风格偏默认、交互细节不足、信息组织没有贴合"搭建平台"的工作流。

本次 UI 换新的目标：

- 从当前默认风格切换为**浅色工具风**，薄荷绿/天蓝 + 黑白灰配色。
- 重新组织三栏布局：**左侧背包栏/选型设置、右侧属性栏、中央画布、顶部层级切换**。
- 以**低信息密度**为默认原则，关键信息清晰可见，细节进属性栏。
- 增强中央画布交互：中键平移、滚轮缩放、左键放置、右键消除、双击属性、拖动批量编辑。
- **逐步替换**现有组件，始终复用现有 Worker 命令、协议与 WebGL 渲染器，不重写模拟核心。

## 2. 设计原则

1. **浅色工具风**：白/浅灰底，柔和阴影，卡片化，圆角适中，克制装饰。
2. **薄荷绿/天蓝为主强调色，黑白灰为中性色**：三层主题与主色对应但互不喧宾夺主。
3. **低密度优先**：默认只展示必要信息，复杂状态放右侧上下文属性栏。
4. **操作即反馈**：所有编辑、运行、保存、导出结果用 Toast 提示，错误记录进底部日志。
5. **一致性**：按钮、卡片、表单、选项卡共用一套设计变量（色彩、圆角、间距、阴影）。
6. **可验证**：每次替换保持 `vitest`、`tsc`、`vite build` 全绿。

## 3. 视觉规范

### 3.1 色彩令牌

```text
--bg-app            #f6f8f7   应用背景（浅灰）
--bg-panel          #ffffff   面板/卡片背景
--bg-hover          #eef4f2   悬停背景
--bg-active         #e0efe9   选中背景
--text-primary      #1f2933   主要文字
--text-secondary    #5b6b66   次要文字
--text-muted        #93a39d   弱化文字
--line              #e3e9e6   分割线/描边

--accent-mint       #3ecfae   薄荷绿（主强调）
--accent-mint-soft  #d9f5ee
--accent-sky        #5aa8f0   天蓝（次强调）
--accent-sky-soft   #e3f0fc
--success           #2fbf8f
--warning           #e8a23b
--danger            #e35d5d

--layer-cluster     #3ecfae   团簇层
--layer-material    #5aa8f0   物质层
--layer-light       #e8c64f   光层
```

### 3.2 尺寸与间距

```text
间距基准：4px
面板内边距：16px
面板间距：12px
卡片圆角：10px
按钮圆角：8px
输入框圆角：8px
选中描边：2px
柔和阴影：0 1px 3px rgba(31,41,51,0.08), 0 4px 12px rgba(31,41,51,0.06)
```

### 3.3 字体

```text
字体栈：system-ui, -apple-system, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif
正文：13-14px
面板标题：13px 半粗
属性值：12-13px 等宽可选（数字、坐标、Tick 用等宽 tabular-nums）
```

### 3.4 三层主题映射

- 团簇层选项卡、选中高亮使用薄荷绿。
- 物质层使用天蓝。
- 光层使用暖金。
- 顶部选项卡与画布图层的色彩提示保持三层一致。

## 4. 布局结构

```text
┌────────────────────────────────────────────────────────┐
│ 顶部栏：品牌 | 层级选项卡 | 运行控制一排 | Tick 读数      │
├────────┬──────────────────────────────┬────────────────┤
│ 左栏    │ 中央画布                     │ 右栏            │
│ 背包栏   │                            │ 上下文属性栏     │
│ · 工具   │ WebGL 画布                  │ · 选中对象属性    │
│ · 选型卡片│ 中键拖动/滚轮缩放/左键放/     │ · 未选中→运行状态 │
│ · 图层  │ 右键清除/双击属性/批量编辑     │ · 导出/操作      │
├────────┴──────────────────────────────┴────────────────┤
│ 底部：Toast（右下角） + 可展开日志栏                      │
└────────────────────────────────────────────────────────┘
```

### 4.1 顶部栏

- 左侧：品牌标识 `ClusterCA Web V2`。
- 中部：三层级选项卡（团簇层 / 物质层 / 光层），点击切换主视图。
- 右侧：运行控制一排（运行/暂停、单步、Tick 速率滑块、Tick 读数）。
- 项目操作（新建、保存、载入、恢复存档、撤销/重做）收敛为右上角"项目"按钮组或菜单，保持一排整洁。

### 4.2 左侧背包栏

上半部分：**工具列表**（选择、平移、胞团、器官、物质、擦除）。

下半部分：**选型卡片区**，根据当前工具动态显示：

- 器官工具 → 器官卡片网格（代码 + 名称 + 颜色块 + 方向选择）。
- 胞团工具 → 尺寸预设卡片（3×3、5×5、7×7，可自定义宽高）。
- 物质工具 → 数值预设（物质/能量量级选择）。
- 图层开关：团簇层 / 物质层 / 光层 三个开关。

卡片规则：

```text
默认：白色卡片 + 浅描边
悬停：浅背景 + 阴影
选中：主色描边 2px + 浅主色背景
```

### 4.3 中央画布

- 中键拖动 = 平移画布。
- 滚轮 = 以鼠标为中心缩放。
- 左键单击 = 按当前工具放置（胞团/器官/物质）。
- 左键拖拽 = 批量放置/批量编辑当前工具。
- 右键单击 = 消除/清空（清除胞团格或器官、清空物质格），并支持右键拖拽批量清除。
- 双击 = 查看对象属性（切换到属性栏并选中）。
- 悬停 = 显示坐标提示（HUD）。
- 跨环面对象在边缘显示镜像片段（保留现有渲染器能力）。

### 4.4 右侧上下文属性栏

- **未选中**：显示全局运行状态（胞团数、种子数、非空物质格、运行状态、总资源摘要）。
- **选中团簇**：显示矩形、坐标、HP、物质、能量、计数器、器官网格摘要、基因指针、群组信息。
- **选中种子**：显示位置、休眠倒计时、基因头、母体 ID。
- 底部常驻操作区：导出（指标 CSV、选区 JSON、截图 PNG）。

属性表单首版只展示已由 Worker 快照提供的数据；可写属性编辑在后续阶段接入。

## 5. 组件清单

| 组件 | 说明 |
|---|---|
| `TopBar` | 品牌、层级选项卡、运行控制、项目操作 |
| `LayerTabs` | 三层级选项卡（团簇/物质/光） |
| `RunControls` | 运行/暂停、单步、速率滑块 |
| `ProjectMenu` | 新建/保存/载入/恢复/撤销/重做 |
| `BackpackPanel` | 左侧背包栏容器 |
| `ToolList` | 工具列表按钮 |
| `SelectionCards` | 按工具动态的选型卡片区 |
| `LayerSwitches` | 图层显示开关 |
| `CanvasViewport` | 画布容器、HUD、手势处理 |
| `ContextInspector` | 右侧上下文属性栏 |
| `ClusterInspector` | 团簇属性详情 |
| `SeedInspector` | 种子属性详情 |
| `WorldStatus` | 全局运行状态摘要 |
| `ExportActions` | 导出操作区 |
| `ToastHost` | 右下角轻提示 |
| `LogPanel` | 底部可展开日志栏 |
| `NewProjectModal` | 新建项目模板弹窗 |

基础控件（自研，纳入 `src/ui` 下组件基元）：

```text
Button、IconButton、Card、Select、RangeInput、NumberInput
Toggle、Tabs、Badge、Field（标签+控件）、PanelSection
```

## 6. 交互规范

- 所有编辑动作先暂停模拟（沿用现有 `pause` 命令约定）。
- 所有编辑动作自动触发存档，动作结束后 Toast 提示结果。
- 错误统一进入日志栏，重级错误同时 Toast 展示。
- 拖动批量放置采用节流发送命令，避免高频 postMessage 堆积。
- 右键菜单首版只做"清除"，后续再扩展上下文菜单。
- 键盘快捷键（预留）：`Space` 平移、`V` 选择、`B` 胞团、`O` 器官、`M` 物质、`E` 擦除、`Esc` 取消。

## 7. 状态与反馈

- **Toast**：右下角堆叠，3-4 秒自动消失，类型含成功/信息/警告/错误。
- **日志栏**：底部可展开/收起，记录编辑、运行、导入、导出、回滚与错误，最多保留 100 条。
- **运行状态**：顶部 Tick 读数实时更新；暂停时画布边缘显示"已暂停"弱提示。

## 8. 接入策略（逐步替换）

1. 保留 `WorkerCommand` / `WorkerEvent` 协议与 `simulation.worker.ts` 不变。
2. 保留 `WebGLWorldRenderer`，仅替换其外层容器和 HUD。
3. 新增 `src/ui` 组件基元，新 UI 从这些基元组装。
4. 按区块替换：先顶部栏与左右面板，再画布交互，最后日志与弹窗。
5. 每个替换区块完成后运行 `vitest run`、`tsc -b`、`vite build` 确认全绿。
6. 对需要新增的能力（右键清除、拖动批量编辑、双击属性），在协议层新增最小命令并补测试。

## 9. 实施阶段

### 阶段 0：设计变量与基元

- 建立 `src/ui/theme.css` 与 CSS 变量。
- 建立 `Button / Card / Select / Tabs / Toggle / Field / Toast` 基元。
- 建立 Storybook 式的手工示例页或临时页面验证组件样式。
- 验收：基元可独立渲染，风格统一。

### 阶段 1：顶部栏与层级选项卡

- 重构 `TopBar`：品牌、层级选项卡、运行控制一排、项目操作。
- 选项卡切换通过现有 `LayerVisibility` 状态与渲染器打通。
- 验收：顶部视觉换新，运行/暂停/单步/速率可用，层级切换生效。

### 阶段 2：左侧背包栏

- 重构左栏为工具列表 + 选型卡片区 + 图层开关。
- 器官选型卡片绑定现有 `set-organelle` 命令。
- 胞团尺寸预设绑定 `add-cluster`。
- 验收：工具切换、选型卡片选中态、图层开关均可用。

### 阶段 3：画布交互增强

- 中键平移、滚轮缩放（沿用现有实现并统一到手势层）。
- 左键放置、左键拖拽批量放置。
- 右键清除、右键拖拽批量清除（协议新增最小命令）。
- 双击查看属性。
- 验收：交互符合第 4.3 节规范，绘制/清除高效且不卡顿。

### 阶段 4：右侧上下文属性栏

- 上下文切换：未选中显示 `WorldStatus`，选中显示对应 Inspector。
- 连接现有快照数据。
- 验收：选中与取消选中时属性栏内容正确切换。

### 阶段 5：反馈与弹窗

- 接入 `ToastHost` 与 `LogPanel`。
- 新建项目弹窗换成新视觉。
- 验收：编辑/错误/导出均有 toast 或日志反馈。

### 阶段 6：清理与收尾

- 删除旧样式与旧组件，统一走新基元。
- 最终三项验证全绿。
- 更新 README 与总体方案中的界面章节。

## 10. 验收标准

- 视觉符合浅色工具风与薄荷绿/天蓝配色规范。
- 三栏布局与顶部层级切换符合设计稿。
- 画布支持中键平移、滚轮缩放、左键放置、右键清除、双击属性、拖动批量编辑。
- 右键清除与批量编辑不影响防重叠与确定性约束。
- `vitest run`、`tsc -b`、`vite build` 全部通过。
- 保存/加载/自动存档仍正常工作。

## 11. 设置系统

顶部栏增加"设置"按钮，打开设置面板，配置持久化到 `localStorage`，变更即时应用到界面。

### 11.1 设置项

| 设置项 | 说明 | 默认值 |
|---|---|---|
| 背景颜色 | 预设主题（深色/浅色/薄荷/天蓝）或自定义色值，写为 `--theme-bg` | 深色 |
| 亮度 | 滑杆 0.8–1.3，作用于画布与面板整体亮度 | 1.0 |
| 字体大小 | 滑杆 0.85–1.3，作用于界面文本缩放 | 1.0 |
| 语言 | 中文 / English，界面文案即时切换 | 中文 |
| 自动保存间隔 | 秒，控制自动存档定时器（最小 10 秒） | 60 秒 |
| 导出路径 | 自动下载（默认）/ 选择目录（File System Access API，Chrome 支持） | 自动下载 |

### 11.2 主题变量

```css
:root {
  --theme-bg: #07100e;
  --theme-brightness: 1;
  --theme-font-scale: 1;
}
```

- 背景：`body`、`.app-shell` 使用 `var(--theme-bg)`。
- 亮度：`.app-shell` 使用 `filter: brightness(var(--theme-brightness))`。
- 字体：界面文本以 `em` 相对 `.app-shell` 基准字号缩放。

### 11.3 国际化

- `src/settings/i18n.ts` 提供中英字典与 `t(key)`。
- 界面文案统一走字典，切换语言即时重渲染。
- 日志与错误信息以中文为主，UI 控件文案双语。

### 11.4 导出路径

- 默认使用现有 `URL.createObjectURL` 自动下载。
- 选择目录后通过 File System Access API 写入选定目录。
- 浏览器不支持或未授权时回退自动下载并在日志提示。

## 12. 版本号与作者署名

- 顶部品牌区展示 `v0.2.0` 与作者 `Anshonesoo`。
- `index.html` 标题、README 同步维护版本与作者。
- 版本常量集中在 `src/settings/settings.ts`（`VERSION`、`AUTHOR`）。

## 13. 不做与后续

首版不做：

- 不引入第三方组件库（自研轻量组件）。
- 不做复杂右键上下文菜单，只做清除。
- 不重写 Worker 与渲染器。
- 不新增性能统计面板。

后续扩展：

- 可写属性编辑（器官参数、资源数值、运动计数）。
- 基因与编译片段可视化编辑。
- 右键上下文菜单、快捷键完整方案。
- 主题色自定义与深浅色切换。
